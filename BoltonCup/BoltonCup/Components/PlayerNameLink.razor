@namespace BoltonCup.Components

@if (Player is not null)
{
    <Blazorise.Link Unstyled To=@($"/player?name={Player.Name}")>
    @Player.Name
</Blazorise.Link>
}


@code {
    [Parameter] public TeamPlayer? Player { get; set; } = null;
}