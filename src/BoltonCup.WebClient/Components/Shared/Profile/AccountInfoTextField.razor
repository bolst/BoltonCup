@using System.Linq.Expressions
@namespace BoltonCup.WebClient.Components.Shared


<MudTextField 
    T="string?"
    Value="@Value"
    ValueChanged="@ValueChanged"
    For="@ValueExpression"
    Label="@Label"
    Variant="@Variant"
    Disabled="@Disabled"
    Mask="@Mask"
    AdornmentText="@AdornmentText"
    Adornment="@Adornment"
    ErrorText="@ErrorText"
    ErrorTextChanged="@ErrorTextChanged"
    Style="color: var(--mud-palette-text-primary)"
    ShrinkLabel="true"
    Margin="Margin.Dense"
    Underline="false" 
    OnlyValidateIfDirty
    Immediate
    />

<style>
    .mud-input.mud-input-filled {
        border-radius: var(--mud-default-borderradius);
    }
</style>
    
@code {
    
    [Parameter]
    public string? Value { get; set; }
    
    [Parameter]
    public EventCallback<string?> ValueChanged { get; set; }
    
    [Parameter]
    public Expression<Func<string?>>? ValueExpression { get; set; }

    [Parameter]
    public string Label { get; set; }
    
    [Parameter]
    public bool Disabled { get; set; }
    
    [Parameter]
    public IMask? Mask { get; set; }

    [Parameter]
    public string? AdornmentText { get; set; }

    [Parameter]
    public string? ErrorText { get; set; }
    
    [Parameter]
    public EventCallback<string?> ErrorTextChanged { get; set; }

    private Adornment Adornment => 
        string.IsNullOrEmpty(AdornmentText) 
            ? Adornment.None 
            : Adornment.End;


    private Variant Variant => Disabled ? Variant.Text : Variant.Filled;

}