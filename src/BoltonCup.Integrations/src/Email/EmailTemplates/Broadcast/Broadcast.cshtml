@using BoltonCup.Integrations.Email.EmailTemplates
@model BroadcastEmailViewModel
@{
    // ReSharper disable once Razor.LayoutNotResolved
    if (Model.UseLayout)
    {
        Layout = EmailConstants.SharedLayout;
    }
}

@Raw(Model.BodyHtml)
